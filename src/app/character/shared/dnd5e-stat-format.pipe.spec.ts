import { Dnd5eStatFormatPipe } from './dnd5e-stat-format.pipe';

describe('Dnd5eStatFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new Dnd5eStatFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
